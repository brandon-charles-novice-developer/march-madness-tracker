[pytest]
testpaths = tests
markers =
    integration: tests that hit external APIs or validate live data files
    e2e: end-to-end tests that run the full pipeline or check deployed site
addopts = -v --tb=short
